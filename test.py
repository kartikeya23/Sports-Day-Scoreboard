import resultsEvaluation as re

hr = ["blue", "green", "red", "yellow"]
finalPoints = [12, 4, 7, 9] # r, b, g, y

# print(re.posPoints, re.houses)
finalPoints = re.addResults(hr, finalPoints)
print(finalPoints)
